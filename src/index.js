import ReactDOM from "react-dom"
import App from "./App"

// COMPONENTE: função com letra maiúscula que retorna um layout (JSX)
// O componente pode sert chamado com a sintaxe de tags

ReactDOM.render(<App/>, document.querySelector(".root"))